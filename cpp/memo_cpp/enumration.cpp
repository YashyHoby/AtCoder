#include "template.h"

//-----------------------------------------------------
//bit全探索 
// 固定bit数
for (int bit = 0; bit < (1 << 3); bit++) {
  bitset<3> bits(bit);
  // ビット列の右端から捜査，1ならif実行
  for (int i = 0; i < N; i++) {
    if (bits.test(i)) {

    }
  }
}

// 可変bit数
// 2^n
for (int bit = 0; bit < (1<<n); bit++) {
  // ビット列の右端から捜査，1ならif実行
  for (int i = 0; i < n; i++) {
    if(bit & (1<<i)){
    }
  }
}
//部分和，ナップサック
//-----------------------------------------------------

